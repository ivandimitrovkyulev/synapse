## For screening
var="$(cat input.json)"
docker run --name="synapse" -it -d "eu.gcr.io/hip-orbit-347017/synapse" python3 main.py -s "$var"

## To run docker container checker
docker cp synapse:/synapse/.env . | chmod go-rw .env
nohup python3 container_check.py synapse &

# docker cp synapse:/synapse/logs/error.log .
# docker cp synapse:/synapse/logs/arbitrage.log .
