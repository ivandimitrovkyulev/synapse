### For screening via APIs
var="$(cat input.json)"
docker run --name="synapse_api" --shm-size="2g" -it -d "eu.gcr.io/hip-orbit-347017/synapse_api" python3 api.py -s "$var"
# docker cp synapse:/synapse_api/logs/error.log .

# To run docker container checker
# docker cp synapse_api:/synapse_api/.env . | chmod go-rw .env
# nohup python3 container_check.py synapse_api &


### For screening via web scrapper
var="$(cat input.json)"
docker run --name="synapse_web" -it -d "eu.gcr.io/hip-orbit-347017/synapse_web" python3 web.py -s "$var"
# docker cp synapse:/synapse_web/logs/error.log .

# To run docker container checker
# docker cp synapse_api:/synapse_web/.env . | chmod go-rw .env
# nohup python3 container_check.py synapse_web &